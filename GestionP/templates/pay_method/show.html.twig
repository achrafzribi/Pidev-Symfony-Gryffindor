{% extends 'base.html.twig' %}

{% block title %}PaiementMethod{% endblock %}

{% block body %}
    <h1>PaiementMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement_method.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ paiement_method.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pay_method_index') }}">back to list</a>

    <a href="{{ path('app_pay_method_edit', {'id': paiement_method.id}) }}">edit</a>

    {{ include('pay_method/_delete_form.html.twig') }}
{% endblock %}
