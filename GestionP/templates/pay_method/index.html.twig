{% extends 'base.html.twig' %}

{% block title %}PaiementMethod index{% endblock %}

{% block body %}
    <h1>PaiementMethod index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paiement_method in paiement_methods %}
            <tr>
                <td>{{ paiement_method.id }}</td>
                <td>{{ paiement_method.Type }}</td>
                <td>
                    <a href="{{ path('app_pay_method_show', {'id': paiement_method.id}) }}">show</a>
                    <a href="{{ path('app_pay_method_edit', {'id': paiement_method.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_pay_method_new') }}">Create new</a>
{% endblock %}
