<form method="post" action="{{ path('app_infotrafic_delete', {'id': infotrafic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infotrafic.id) }}">
    <button >
      Delete
    </button>
</form>
